apiVersion: v1
kind: Service
metadata:
  name: gdgkl-backend-rabbitmq
spec:
  selector:
    app.kubernetes.io/name: gdgkl-backend-rabbitmq
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
