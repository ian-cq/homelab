apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: argocd
  namespace: argocd
spec:
  parentRefs:
    - name: public
      namespace: cilium-system
  hostnames:
    - argocd.62a.quanianitis.com
  rules:
    - backendRefs:
        - name: argocd-server
          port: 80
